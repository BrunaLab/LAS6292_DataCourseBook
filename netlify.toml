[build]
  publish = "_site"

[[plugins]]
  package = "@quarto/netlify-plugin-quarto"

  [plugins.inputs]
    render = true
    # If you need TinyTeX for PDF outputs, you can request it here if supported:
    tinytex = true